name: CI

on:
  push:
    branches: [ main, dev]
  pull_request:

permissions:
  checks: write
  pull-requests: write

jobs:
  contracts:
    name: Contracts
    uses: TerraDharitri/drt-sc-actions/.github/workflows/contracts.yml@v2.0.0
    with:
      rust-toolchain: stable
      drt-go-scenario-version: v5.1.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
